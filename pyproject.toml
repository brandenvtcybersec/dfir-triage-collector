[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dfir-triage-collector"
version = "0.1.0"
description = "Read-only DFIR triage collector (Windows-first)"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
dfir-triage = "dfir_triage.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}